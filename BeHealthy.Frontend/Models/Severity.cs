namespace BeHealthy.Frontend.Models;

// Ported from BeHealthy.Domain so the WASM frontend does not depend on the Domain project.
public enum Severity
{
    Info,
    Warning,
    Success,
    Danger
}
